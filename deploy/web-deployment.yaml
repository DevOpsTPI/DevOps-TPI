apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
spec:
  replicas: 2   # 🔁 dos réplicas, balanceadas por Traefik
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
